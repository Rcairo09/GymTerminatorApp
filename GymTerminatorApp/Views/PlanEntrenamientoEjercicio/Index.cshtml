@model IEnumerable<GymTerminatorApp.Models.PlanEntrenamientoEjercicio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ejercicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanEntrenamiento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ejercicio.EjercicioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlanEntrenamiento.PlanEntrenamientoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlanEntrenamientoEjercicioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlanEntrenamientoEjercicioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlanEntrenamientoEjercicioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
